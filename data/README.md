# Ediciones diarias

Un archivo `YYYY-MM-DD.json` por edición, generado por el pipeline. Cada uno contiene las noticias del día (`items`) y, cuando los hay, los repositorios hot (`repos`). El formato se valida contra [`../schema/digest.schema.json`](../schema/digest.schema.json).
